package com.dondi.prueba.session.exception;
/*
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ControllerAdvice;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.ui.Model;*/

//@ControllerAdvice
public class GlobalException {
    /*
     * // Manejar error 404 (Recurso no encontrado)
     * 
     * @ExceptionHandler(Exception.class)
     * public ResponseEntity<Object> handleAllErrors(Exception ex, Model model) {
     * String message = "Recurso no encontrado: " + ex.getMessage();
     * ErrorText errorText = new ErrorText(message,
     * "Verifique la URL o los parámetros proporcionados.");
     * return new ResponseEntity<>(errorText, HttpStatus.NOT_FOUND);
     * }
     */

}
